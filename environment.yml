name: CSAI
channels:
  - defaults
dependencies:
  - python=3.11
  - ipykernel
  - matplotlib
  - numpy
  - pandas
  - scikit-learn
  - seaborn
  - pip
  - pip:
      - sentence-transformers   # Model 2 embeddings (pulls in torch)
      - nbconvert               # headless notebook execution (optional)
